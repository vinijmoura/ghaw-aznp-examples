# Workflow Documentation

This folder contains auto-generated documentation for the GitHub Actions workflows defined in `.github/workflows/`.

Each YML workflow file has a corresponding markdown file here that describes its purpose, triggers, jobs, and step-by-step details.

> **Note**: These files are automatically created and updated by the `workflow-docs-generator` workflow whenever workflow YML files are changed.
